package com.storeflow.storeflow_api.entity.enums;

/**
 * Product status values exposed through the GraphQL API and product responses.
 */
public enum ProductStatus {
    ACTIVE,
    INACTIVE,
    DISCONTINUED
}